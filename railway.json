{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "sh -c 'mkdir -p build/web build/web-cache && uv run pygbag --build --ume_block 0 . && python -m http.server ${PORT:-8000} --directory build/web'"
  }
}
